{
  "name": "@seneca/tillo-provider",
  "version": "1.0.0",
  "main": "dist/TilloProvider.js",
  "type": "commonjs",
  "types": "dist/TilloProvider.d.ts",
  "description": "Seneca entity provider for the Tillo API.",
  "homepage": "https://github.com/senecajs/seneca-tillo-provider",
  "keywords": [
    "seneca",
    "tillo",
    "tillo-provider"
  ],
  "author": "Richard Rodger - richardrodger.com",
  "repository": {
    "type": "git",
    "url": "git://github.com/senecajs/seneca-tillo-provider.git"
  },
  "scripts": {
    "prettier": "prettier --write --no-error-on-unmatched-pattern src test",
    "test": "node --enable-source-maps --test dist-test/TilloProvider.test.js",
    "test-some": "node --enable-source-maps --test --test-name-pattern",
    "watch": "tsc -b -w",
    "build": "tsc -b",
    "doc": "echo seneca-doc -p @seneca/provider FIX-broken",
    "clean": "rm -rf node_modules dist dist-test yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run doc && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "files": [
    "dist",
    "src/**/*.ts",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22"
  },
  "dependencies": {},
  "peerDependencies": {
    "@seneca/env": ">=0.2",
    "@seneca/provider": ">=3",
    "seneca": ">=3||>=4.0.0-rc2",
    "seneca-entity": ">=26",
    "seneca-promisify": ">=3"
  },
  "devDependencies": {
    "@hapi/code": "^9.0.3",
    "@seneca/doc": "^8.0.0",
    "@seneca/env": ">=0.2",
    "@seneca/maintain": "^0.1.0",
    "@seneca/provider": ">=3",
    "@types/node": "^22.0.0",
    "esbuild": "^0.17.7",
    "prettier": "^3.8.1",
    "seneca": ">=3",
    "seneca-entity": ">=26",
    "seneca-msg-test": "^3.4.0",
    "seneca-promisify": ">=3",
    "typescript": "^5.9.3"
  }
}
